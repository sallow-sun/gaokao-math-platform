# 数海 MathSea 新版后端与 API 对接说明

> 面向：第一次参与后端开发的开发者
>
> 对接前端：`frontend-vue/`（Vue 3 + Vite）
>
> 最后更新：2026-08-28

## 1. 先读这一段

你好，感谢你参与数海 MathSea 的后端开发。

这份文档不是要求你立刻写出完整后端，也不是让你一次性实现所有功能。它的作用是帮助前后端先就以下内容达成一致：

1. 前端需要哪些真实数据和操作。
2. API 的地址、参数、返回字段和错误格式是什么。
3. 哪些数据属于用户账号，哪些设置只保存在浏览器。
4. 账号、密码、题目和个人备注应如何安全保存。
5. 如何把工作拆成可以逐步检查的小阶段。

请不要收到文档后直接修改旧 `app.py`，也不要先凭感觉设计数据库。第一步是阅读本文并完成第 18 节的“后端确认回复”。双方确认后，再开始创建新版后端。

---

## 2. 用简单的话解释：后端和 API 是什么

### 2.1 前端负责什么

前端是学生在浏览器中看到和操作的页面，例如：

- 登录和注册表单；
- 题库筛选按钮；
- 题目详情；
- 收藏、已做和加入题单按钮；
- 个人资料和安全设置。

前端不应该直接连接数据库，也不能安全地保存密码。

### 2.2 后端负责什么

后端运行在服务器上，主要负责：

- 验证用户身份；
- 安全保存账号和密码哈希；
- 从数据库查询题目；
- 保存收藏、已做、题单和备注；
- 判断当前用户是否有权修改某份数据；
- 返回统一、可预测的结果；
- 记录错误、执行备份和数据库迁移。

### 2.3 API 是什么

API 是前端和后端之间约定好的通信方式。

例如，学生打开题库时，前端会向后端请求“第 1 页、每页 20 道、2025 年、单选题”。后端查询数据库后，用 JSON 返回题目和分页信息。

前端不关心后端使用 Python、Java、Go 还是 Node.js。前端真正依赖的是：

- 请求地址；
- HTTP 方法；
- 参数名称；
- 返回字段；
- 登录方式；
- 成功和失败时的状态码；
- 字段含义长期保持稳定。

这套约定统称为“API 契约”。应先确定契约，再分别编写前端和后端实现。

---

## 3. 当前项目状态

当前主要前端是 `frontend-vue/`。旧 `frontend/` 只作为视觉和交互参考；旧 `templates/` 和 `app.py` 不是新版 Vue API 的架构依据。

目前 Vue 前端中的数据状态如下：

| 功能 | 当前状态 | 正式上线后的归属 |
| --- | --- | --- |
| 题库题目 | 只有少量前端原型题 | 后端数据库与题目 API |
| 收藏 | 保存在当前浏览器 | 登录用户的后端数据 |
| 已做 | 保存在当前浏览器 | 登录用户的后端数据 |
| 个人题单 | 保存在当前浏览器 | 登录用户的后端数据 |
| 题单备注和顺序 | 保存在当前浏览器 | 登录用户的后端数据 |
| 登录、注册 | 静态页面，未提交数据 | 后端认证与会话 |
| 个人主页 | 示例数据 | 用户公开资料 API |
| 答案和解析 | 待接入 | 题目详情数据 |
| 官方题单、题单广场 | 待接入 | 第二阶段后端功能 |
| 论坛、讨论、推荐 | 待接入 | 后续阶段，不在第一版范围内 |
| 主题、打印排版、显示选项 | 浏览器偏好 | 可以继续保存在浏览器 |

重要边界：本地收藏、已做和题单目前只是 API 接入前的原型，不能将它们描述为账号云同步。

---

## 4. 推荐的技术方案

如果没有已经确定的后端技术栈，建议使用：

- 语言：Python；
- Web 框架：Django；
- API 框架：Django REST Framework；
- 数据库：PostgreSQL；
- API 风格：REST + JSON；
- 接口版本前缀：`/api/v1`；
- 架构：一个模块化单体后端；
- 认证：服务器会话 + 安全 Cookie；
- API 文档：OpenAPI/Swagger；
- 用户头像和题目图片：文件存储或对象存储，不直接存入数据库正文。

选择 Django 的原因：本项目不仅需要读取题目，还需要账号、权限、管理后台、数据库迁移、文件上传和内容管理。Django 已经提供这些基础能力，比从多个零散组件拼装更适合第一次开发后端的团队。

如果开发者已经非常熟悉另一套成熟框架，可以提出替代方案。但替代方案必须仍然满足本文中的 API、安全、数据库迁移、测试和交付要求。不要仅仅因为 Vue 使用 JavaScript，就认为后端也必须使用 JavaScript。

第一版不要引入微服务、消息队列、Elasticsearch、Kubernetes 等额外复杂度。只有出现明确性能或业务需求时，再讨论增加基础设施。

建议新后端使用独立目录，例如 `backend/`。在新版架构确认前，不覆盖旧 `app.py`，不直接修改现有 `.db` 文件。

---

## 5. 开始开发前必须共同确认的业务问题

下面的问题会影响数据库和 API。后端不应自行猜测，产品负责人也不应把它们全部交给开发者决定。

| 问题 | 当前建议 | 状态 |
| --- | --- | --- |
| 未登录能否浏览题库和题目详情 | 可以浏览；写入个人数据时要求登录 | 待确认 |
| 注册必填项 | 用户名、邮箱、密码；手机号可选 | 待确认 |
| 邮箱是否必须验证 | 建议验证后才允许用于找回密码 | 待确认 |
| 支持哪些登录标识 | 用户名、公开 UID、已验证邮箱；手机号需验证后才可用 | 待确认 |
| 密码最低要求 | 至少 8 个字符，并设置合理最大长度 | 待确认 |
| 用户公开地址使用什么 ID | 建议使用独立、不可预测的公开 ID，不暴露数据库主键 | 待确认 |
| “已做”的含义 | 用户手动标记，不代表答案正确 | 待确认 |
| 难度颜色顺序和含义 | 必须形成固定枚举和说明 | 待确认 |
| 题目答案是否对所有访客开放 | 当前建议题目详情可查看，后续可增加策略 | 待确认 |
| 个人题单默认是否私有 | 建议默认私有 | 待确认 |
| 本地题单是否支持首次登录导入 | 建议后续提供一次明确的导入选择 | 待确认 |
| 用户能否上传题目 | 第一阶段不开放 | 建议确定 |

只有“状态”变为已确认后，才能把这些内容固化到接口和数据库中。

---

## 6. 第一阶段范围

第一阶段只实现让现有 Vue 页面获得真实数据所必需的功能。

### 6.1 必须完成

1. 用户注册、登录、退出和当前会话查询。
2. 安全的邮箱密码重置流程。
3. 用户公开资料和自己的资料修改。
4. 头像上传。
5. 修改密码；用户名、邮箱和手机号修改流程可以按验证方案逐步实现。
6. 题库列表、筛选、排序和分页。
7. 题目详情、答案和解析。
8. 随机题目。
9. 收藏和已做状态。
10. 我的题单：创建、查看、编辑、删除和设置默认题单。
11. 题单中批量添加、移除和排序题目。
12. 题单内每道题的个人备注。
13. 后台管理题目、标签、来源和用户的基础能力。

### 6.2 第一阶段不做

- 论坛和题目讨论；
- 题目推荐算法；
- 题单广场和公开分享；
- 用户投稿及复杂审核；
- 答题判分和正确率系统；
- 排行榜、积分和付费功能；
- 微服务拆分。

页面上虽然已经预留了部分入口，但不能为了让按钮“看起来可用”而返回虚构数据。

---

## 7. 通用 API 约定

这些规则应应用于所有接口。

### 7.1 地址和版本

- 所有新版接口使用 `/api/v1` 前缀。
- API 只返回 JSON；文件上传接口除外，文件上传使用 `multipart/form-data`。
- API 请求失败时也返回 JSON，不返回 Flask/Django HTML 错误页。
- 前端不得写死开发服务器地址；开发和生产地址通过环境配置或同域 `/api` 代理处理。

### 7.2 字段命名

建议 API 使用 `camelCase`，与现有 Vue 数据一致，例如：

- `problemId`
- `createdAt`
- `sourceLabel`
- `isDefault`
- `pageSize`

数据库内部可以使用 Python 和 SQL 常见的 `snake_case`，但不能把数据库字段名未经设计直接暴露给前端。

### 7.3 ID 和时间

- API 中所有 ID 按字符串处理，即使数据库内部使用整数。
- 题目编号必须稳定，例如 `P10001`，不能因数据库迁移而变化。
- 所有时间使用 ISO 8601 格式并保存为 UTC，例如 `2026-08-28T10:30:00Z`。
- 由后端生成创建时间和更新时间，不接受前端伪造。

### 7.4 空值

- 没有标签时返回空数组 `[]`，不要返回 `null`。
- 没有题单时返回空数组 `[]`。
- 只有确实存在“未知/未设置”含义时才使用 `null`。
- 不返回前端需要自行解析的数据库字符串，例如把数组塞进逗号分隔文本。

### 7.5 常用状态码

| 状态码 | 含义 |
| --- | --- |
| `200` | 查询或修改成功 |
| `201` | 创建成功 |
| `204` | 删除成功且无需返回正文 |
| `400` | 请求格式或业务参数错误 |
| `401` | 未登录或会话失效 |
| `403` | 已登录，但无权执行该操作 |
| `404` | 对象不存在，或用户无权知道其存在 |
| `409` | 用户名、邮箱等唯一值冲突 |
| `413` | 上传文件过大 |
| `422` | 字段校验失败；如果选用该约定需全项目统一 |
| `429` | 请求过于频繁 |
| `500` | 服务器内部错误，不能把堆栈返回给用户 |

不要在错误发生时仍返回 `200`，再让前端从文字中猜测成功或失败。

### 7.6 统一错误格式

所有 API 应使用同一种错误结构。示例：

```json
{
  "error": {
    "code": "VALIDATION_ERROR",
    "message": "请检查填写内容",
    "fieldErrors": {
      "username": ["用户名已被使用"]
    },
    "requestId": "用于日志排查的请求编号"
  }
}
```

- `code`：稳定的英文机器错误码，前端可以据此处理。
- `message`：可直接向用户展示的简体中文说明。
- `fieldErrors`：可选，用于表单字段错误。
- `requestId`：可选但建议提供，便于从日志找到对应请求。

不要把 Python 异常名、SQL、文件路径或堆栈返回给浏览器。

---

## 8. 认证和账户 API

建议的第一版接口如下。路径名称可以在实现前讨论，但功能不能缺失。

| 方法与路径 | 是否登录 | 作用 |
| --- | --- | --- |
| `POST /api/v1/auth/register` | 否 | 创建账号 |
| `POST /api/v1/auth/login` | 否 | 登录并建立会话 |
| `POST /api/v1/auth/logout` | 是 | 注销当前会话 |
| `GET /api/v1/auth/me` | 可选 | 查询当前会话和当前用户 |
| `POST /api/v1/auth/password-reset/request` | 否 | 请求发送密码重置邮件 |
| `POST /api/v1/auth/password-reset/confirm` | 否 | 使用一次性令牌设置新密码 |
| `POST /api/v1/auth/change-password` | 是 | 验证当前密码后修改密码 |

### 8.1 注册输入

建议字段：

- `username`
- `email`
- `phone`，可选
- `password`
- `confirmPassword`

前端会检查两次密码是否一致，后端仍必须重复检查，不能相信前端校验。

### 8.2 登录输入

建议字段：

- `account`：用户名、公开 UID、已验证邮箱或已验证手机号；
- `password`。

登录失败统一显示“账号或密码错误”，不能分别告诉攻击者“账号不存在”或“密码错误”。被封禁账号也要使用经过设计的统一响应，不暴露多余信息。

### 8.3 当前用户

`GET /api/v1/auth/me` 用于：

- 页面刷新后恢复登录状态；
- 导航栏显示头像和用户名；
- 判断是否可以访问个人设置；
- 获取 CSRF 或会话相关状态。

未登录时可以选择返回 `401`，或返回明确的 `authenticated: false`。必须在开发前统一一种方式。

### 8.4 密码重置

密码重置不能只检查“账号 + 邮箱匹配”就直接允许修改密码。正确流程是：

1. 用户提交邮箱或账号。
2. 无论账号是否存在，都返回相同的普通提示。
3. 如果账号存在，向已经验证的邮箱发送随机、一次性、短时间有效的链接或令牌。
4. 用户打开链接后提交令牌和新密码。
5. 后端验证令牌后才修改密码。
6. 令牌使用后立即失效，并使旧会话失效或让用户选择退出其他设备。

旧 `app.py` 中根据账号和邮箱直接重设密码的方式不能迁入正式后端。

---

## 9. 用户资料 API

建议接口：

| 方法与路径 | 作用 |
| --- | --- |
| `GET /api/v1/users/{publicId}` | 获取允许公开的个人主页 |
| `GET /api/v1/users/me` | 获取自己的完整可编辑资料 |
| `PATCH /api/v1/users/me` | 修改签名等普通资料 |
| `POST /api/v1/users/me/avatar` | 上传或替换头像 |
| `DELETE /api/v1/users/me/avatar` | 删除自定义头像 |
| `PATCH /api/v1/users/me/username` | 验证身份后修改用户名 |
| `POST /api/v1/users/me/email-change/request` | 请求验证新邮箱 |
| `POST /api/v1/users/me/email-change/confirm` | 验证令牌后更换邮箱 |
| 手机号变更的请求与确认接口 | 接入短信服务后再启用 |

公开个人资料建议包含：

- `publicId`
- `uid`
- `username`
- `avatarUrl`
- `signature`
- `role`
- `joinedAt`
- `stats`：允许公开的已做、收藏、贡献和连续学习统计；
- `recentActivity`：允许公开的最近活动；没有记录时返回空数组。

公开接口不得返回：

- 密码或密码哈希；
- 完整邮箱；
- 完整手机号；
- 登录时间、IP、会话令牌；
- 私有题单和个人备注；
- 管理员内部备注。

头像第一版沿用前端限制：PNG、JPEG、GIF 或 WEBP，最大 5MB。后端必须检查文件真实类型和大小，并生成新的安全文件名，不能直接使用用户提交的路径。

---

## 10. 题库 API

### 10.1 题库列表

建议接口：

`GET /api/v1/problems`

支持的查询参数：

| 参数 | 含义 |
| --- | --- |
| `keyword` | 搜索题号、题目、知识点或试卷来源 |
| `year` | 年份，可多选 |
| `source` | 来源代码，可多选 |
| `type` | 题型代码，可多选 |
| `level` | 难度代码，可多选 |
| `sort` | 排序方式 |
| `page` | 页码，从 1 开始 |
| `pageSize` | 每页数量，默认和当前前端一致为 20 |

当前前端使用的排序值：

- `newest`：最新收录；
- `oldest`：最早收录；
- `easy-first`：由易到难；
- `hard-first`：由难到易；
- `view-most`：最多浏览；
- `star-most`：最多收藏。

后端不能按难度代码的字母顺序排序，必须使用双方确认的难度顺序。

多选参数建议使用重复查询参数，例如同时传递多个 `year`。也可以选择其他格式，但必须在 OpenAPI 文档中写清楚，并与 Vue Router 的实际序列化方式联调验证。

建议分页返回：

```json
{
  "items": [],
  "pagination": {
    "page": 1,
    "pageSize": 20,
    "total": 0,
    "hasNext": false
  }
}
```

题库的筛选、排序或关键词变化后，前端会从第一页重新加载。后端必须保证分页排序稳定，相同请求不能无故出现重复题目或漏题。

### 10.2 列表题目字段

每道列表题目建议包含：

- `id`：稳定题号，例如 `P10001`；
- `title`；
- `detail`；
- `year`；
- `source`；
- `sourceLabel`；
- `type`；
- `typeLabel`；
- `level`；
- `tags`；
- `sourceText`；
- `content`：题干；
- `stats`：真实统计；
- `viewerState`：当前登录用户的收藏、已做和题单状态。

匿名访问时，`viewerState` 可以为 `null`，或者返回全部为 `false` 并附带未登录标识；必须统一一种方式。

统计字段应使用数字，例如浏览量 `13000`，不要由后端返回 `1.3万`、`13k` 等展示字符串。如何显示由前端决定。没有真实数据时返回零，不得生成虚假数值。

### 10.3 题目详情

建议接口：

`GET /api/v1/problems/{problemId}`

详情除列表字段外，还需要：

- `answer`；
- `solution`；
- `uploader` 的公开信息；
- `createdAt`；
- `updatedAt`；
- 图片或附件信息；
- 当前用户状态。

题库列表不要一次返回所有题目的完整答案和解析。前端打开题目详情或“快速查看题解”时再加载并缓存详情，避免列表响应过大。

### 10.4 随机题目和筛选目录

建议接口：

| 方法与路径 | 作用 |
| --- | --- |
| `GET /api/v1/problems/random` | 返回一个当前可访问的随机题号或题目摘要 |
| `GET /api/v1/problem-catalogs` | 返回年份、来源、题型、难度和标签目录 |
| `GET /api/v1/problem-stats` | 返回已收录题数、真题年份数、专题数等题库概况 |

随机题目不能继续依赖写死在前端里的两个示例题号。

---

## 11. 数学内容格式

题干、答案和解析的内容格式必须在录入大量数据前确定，否则后期迁移成本很高。

第一版建议：

- 普通排版使用受限制的 Markdown；
- 数学公式保存 LaTeX 源文本；
- 图片和几何图作为独立资源，通过 URL 引用；
- 不允许任意用户直接提交未经清理的 HTML；
- 保存 `contentFormat` 或 `contentVersion`，说明内容格式版本；
- 题目选项可以先作为题干的一部分，但需讨论未来是否改成结构化选项数组；
- 数据库保存原始内容，前端负责最终显示，但前后端必须约定公式分隔符和换行规则。

至少用以下类型的真实题目验证格式：

1. 单选题和多选题；
2. 填空题；
3. 多小问解答题；
4. 含行内公式和独立公式的题目；
5. 含图片或几何图的题目；
6. 含表格或分段材料的题目。

---

## 12. 收藏与已做状态 API

当前“已做”是用户手动标记，不表示作答正确。后端不要据此计算正确率。

建议接口：

| 方法与路径 | 作用 |
| --- | --- |
| `PATCH /api/v1/users/me/problem-states/{problemId}` | 更新单题收藏或已做状态 |
| `PATCH /api/v1/users/me/problem-states/batch` | 批量更新，供批量操作使用 |

单题请求可以只提交发生变化的字段，例如 `favorite` 或 `completed`。

要求：

- 同一用户与同一题目只能有一条状态记录；
- 重复设置为同一状态应安全成功，不产生重复数据；
- 题目列表和详情最好直接附带当前用户状态，避免前端为每道题额外发送一次请求；
- 未登录时返回 `401`；
- 不存在的题目返回 `404`；
- 用户只能修改自己的状态。

---

## 13. 个人题单 API

当前题单数据模型已经包含：题单标题、说明、默认题单、题目顺序、每题备注、创建时间和更新时间。

建议接口：

| 方法与路径 | 作用 |
| --- | --- |
| `GET /api/v1/users/me/practice-lists` | 获取我的题单列表 |
| `POST /api/v1/users/me/practice-lists` | 创建题单 |
| `GET /api/v1/users/me/practice-lists/{listId}` | 获取题单详情和有序题目 |
| `PATCH /api/v1/users/me/practice-lists/{listId}` | 修改标题和说明 |
| `DELETE /api/v1/users/me/practice-lists/{listId}` | 删除题单 |
| `PUT /api/v1/users/me/practice-lists/{listId}/default` | 设为默认题单 |
| `POST /api/v1/users/me/practice-lists/{listId}/items` | 批量添加题目 |
| `DELETE /api/v1/users/me/practice-lists/{listId}/items/{problemId}` | 移除单题 |
| `POST /api/v1/users/me/practice-lists/{listId}/items/remove` | 批量移除指定题目 |
| `PUT /api/v1/users/me/practice-lists/{listId}/order` | 保存完整题目顺序 |
| `PATCH /api/v1/users/me/practice-lists/{listId}/items/{problemId}` | 保存该题个人备注 |

题单建议字段：

- `id`
- `title`
- `description`
- `isDefault`
- `createdAt`
- `updatedAt`
- `problemCount`
- `completedCount`
- `items`

题单条目建议字段：

- `problemId`
- `position`
- `note`
- `addedAt`
- `updatedAt`
- 对应题目的展示摘要

要求：

- 用户只能访问和修改自己的私人题单；
- 同一道题在同一题单中不能重复；
- 批量添加时应返回“成功添加”和“原本已存在”的题号；
- 删除题单不会删除题库中的原题；
- 从题单移除题目时，只删除该题在这份题单中的条目和个人备注；
- 删除默认题单后，应明确新的默认题单规则；
- 备注保存失败时必须返回真实错误，不能告诉前端“保存成功”；
- 第一版可以采用最后一次保存覆盖之前内容，但要返回 `updatedAt`，并记录这一冲突策略。

前端备注目前有短延迟自动保存。联调时要检查连续输入、快速切换题目、断网和会话过期。

---

## 14. 建议的数据库实体

下面是概念模型，不是要求直接照抄的 SQL。

| 实体 | 主要职责 |
| --- | --- |
| User | 登录身份、用户名、邮箱、手机号、角色和账号状态 |
| UserProfile | 公开 UID、头像、签名等资料；也可与自定义 User 合并设计 |
| Problem | 题号、题干、答案、解析、年份、题型、难度和来源 |
| ProblemSource | 试卷来源代码和中文名称 |
| Tag | 知识标签 |
| ProblemTag | 题目与标签的多对多关系 |
| UserProblemState | 某用户对某题的收藏和已做状态 |
| PracticeList | 用户题单标题、说明和默认状态 |
| PracticeListItem | 题单中的题目、顺序、个人备注和加入时间 |
| ProblemAsset | 题目图片或附件元数据 |
| AuditLog | 管理员重要操作记录，可在管理功能成熟时增加 |

数据库约束至少应包括：

- 用户名唯一；
- 规范化后的邮箱唯一策略；
- 公开 UID 唯一；
- 题目编号唯一；
- 同一用户与题目只有一条 `UserProblemState`；
- 同一题单与题目只有一条 `PracticeListItem`；
- 题单必须属于用户；
- 题单条目必须引用真实题目；
- 删除规则使用明确的外键策略。

不要通过手工打开数据库文件来增加字段。所有结构变化都必须通过 Django migration，并提交迁移文件。

如果使用 Django 自定义用户模型，应在项目第一次迁移前确定。项目运行后再更换用户模型会非常困难。

---

## 15. 安全要求

这些不是“以后优化”，而是第一版必须遵守的基础要求。

### 15.1 会话

- 推荐使用服务器会话和 `HttpOnly`、`Secure`、合适 `SameSite` 属性的 Cookie。
- 不把会话 ID、JWT、刷新令牌或密码放入 `localStorage`。
- 所有正式环境请求使用 HTTPS。
- 登录成功、密码修改和权限变化后更新会话标识。
- 退出登录后使当前会话立即失效。
- 修改密码后使旧会话失效，或提供退出其他设备的选择。

### 15.2 CSRF 与 CORS

- 使用 Cookie 会话时，所有修改数据的请求必须有 CSRF 防护。
- 生产环境优先让 Vue 和 API 使用同一站点，例如前端访问 `/api/v1/...`。
- 本地开发如果前端和后端端口不同，只允许明确的 Vite 开发地址，并正确配置凭据。
- 允许凭据时不能把 CORS 来源设置为通配符 `*`。
- 后端应把前端如何获取和提交 CSRF 令牌写进联调说明。

### 15.3 密码与账号

- 使用 Django 提供的密码哈希和认证能力，不自行编写加密算法。
- 数据库中永远不保存明文密码。
- 登录、注册、密码重置和验证码接口必须限流。
- 密码重置令牌必须随机、一次有效并设置过期时间。
- 修改密码必须验证当前密码。
- 修改邮箱或手机号必须验证新的邮箱或手机号。
- 错误提示不能帮助攻击者枚举账号。

### 15.4 权限

- 每个私人对象查询都要限制到当前用户，不能只依赖前端隐藏按钮。
- 知道别人的题单 ID 不代表有权读取或修改它。
- 普通用户不能通过修改请求字段把自己变成管理员。
- 管理员敏感操作应记录日志。

### 15.5 上传文件

- 同时检查扩展名、MIME 类型、真实文件内容和大小。
- 服务器重新生成文件名。
- 上传目录不可执行脚本。
- 不把本机绝对路径返回给前端。
- 删除旧头像时防止删除其他用户文件。

---

## 16. 开发顺序和每一步的完成标准

请按顺序推进，不要同时铺开所有模块。

### 阶段 0：确认契约

工作内容：

1. 阅读本文。
2. 回复第 18 节确认模板。
3. 与前端确认业务问题。
4. 起草 OpenAPI 文档和示例数据。

完成标准：前端可以只看文档，就知道每个请求如何发送、成功和失败时会得到什么。

### 阶段 1：后端骨架和数据库

工作内容：

1. 建立独立新版后端目录。
2. 配置开发、测试和生产环境设置。
3. 连接 PostgreSQL。
4. 确定自定义用户模型。
5. 建立迁移和基础测试。
6. 建立统一错误响应和健康检查。

完成标准：新开发者能根据 README 启动服务、运行迁移和测试；没有修改旧数据库文件。

### 阶段 2：账户和会话

工作内容：注册、登录、退出、当前用户、CSRF、密码修改和安全重置。

完成标准：匿名、正常用户、密码错误、被封禁用户、会话过期和限流都有自动化测试。

### 阶段 3：只读题库

工作内容：题目目录、列表、筛选、排序、分页、详情和随机题目。

完成标准：使用不少于 40 道覆盖不同年份、题型、难度和公式格式的测试数据，能够验证至少两页无限加载。

### 阶段 4：收藏和已做

工作内容：单题和批量更新，列表与详情返回用户状态。

完成标准：刷新页面后状态保留，不同用户的数据互不影响，匿名写入返回 `401`。

### 阶段 5：个人题单和备注

工作内容：题单 CRUD、默认题单、批量添加移除、排序和自动保存备注。

完成标准：两个不同用户不能互相读取或修改私人题单；删除和排序行为与前端一致。

### 阶段 6：真实联调与旧数据迁移

工作内容：Vue 接入测试环境、处理错误状态、设计旧 SQLite 数据导入工具和回滚方案。

完成标准：核心人工检查清单通过，备份后可重复执行迁移，失败时不会破坏原数据库。

每完成一个阶段，先让前端联调和验收，再进入下一阶段。

---

## 17. 测试、文档和交付要求

### 17.1 每个接口至少测试

- 正常成功；
- 缺少必填字段；
- 字段格式错误；
- 未登录；
- 已登录但无权访问；
- 对象不存在；
- 唯一值冲突；
- 重复提交；
- 超过允许频率；
- 数据库或外部邮件服务失败时的处理。

权限测试至少准备：

- 一个匿名访问者；
- 普通用户 A；
- 普通用户 B；
- 一个管理员。

特别检查用户 A 不能读取、编辑或删除用户 B 的私人题单和备注。

### 17.2 后端每阶段应交付

- 更新后的 OpenAPI/Swagger 文档；
- 成功和失败响应示例；
- 测试环境地址；
- 测试账号；
- 数据库迁移文件；
- 自动化测试和运行结果；
- 环境变量清单，但不提交真实密钥；
- 从零启动的 README；
- 已知限制和下一步；
- 本阶段变更文件清单。

不得提交到 Git：

- `.env` 和真实密钥；
- 本地数据库文件；
- 用户上传文件；
- 日志；
- 缓存目录；
- 虚拟环境；
- 测试邮件服务、数据库或对象存储的真实密码。

可以提交 `.env.example`，但其中只能包含变量名和安全的示例值，不能包含真实凭据。

### 17.3 API 才算完成的条件

只有同时满足以下条件，接口才算完成：

1. OpenAPI 文档与实际行为一致。
2. 前端可以在测试环境真实调用。
3. 成功、空数据、校验失败、未登录、无权限和服务器错误都有明确结果。
4. 数据在刷新页面后仍然存在。
5. 不同用户之间的数据隔离正确。
6. 自动化测试通过。
7. 没有把密码、令牌、数据库路径或异常堆栈返回给浏览器。

“在 Postman 里成功过一次”不等于接口已经完成。

---

## 18. 后端收到文档后的确认回复模板

请后端在写业务代码前复制下面内容并填写。遇到不懂的地方可以直接写“不确定，需要一起讨论”，不要猜测。

```text
我已经阅读《数海 MathSea 新版后端与 API 对接说明》。

1. 拟采用的语言和框架：
2. 拟采用的数据库：
3. 新版后端计划放置的目录：
4. 登录方案（推荐服务器会话 Cookie）：
5. CSRF 处理方式：
6. 本地开发时前后端如何互相访问：
7. 用户公开 ID 方案：
8. 注册必填项和可登录标识：
9. 邮箱验证与密码重置邮件方案：
10. 题目正文、公式和图片的存储格式：
11. 题库多选筛选参数格式：
12. 题库分页格式：
13. 错误响应格式：
14. 旧 SQLite 数据准备如何迁移：
15. 我建议修改或补充的 API：
16. 目前仍不理解、需要一起讨论的问题：

我会先提交 OpenAPI 草案、数据模型草案和启动说明，确认后再实现业务接口。
```

---

## 19. 前后端沟通方式

每讨论一个接口，请用下面七项描述，不要只说“给我一个题库接口”：

1. 谁在什么页面触发它；
2. 请求方法和地址；
3. 是否必须登录；
4. 请求字段和每个字段的含义；
5. 成功返回字段；
6. 可能失败的情况和状态码；
7. 可验证的完成标准。

如果接口字段需要变更：

1. 先解释为什么变更；
2. 更新 OpenAPI 文档；
3. 给出新旧字段示例；
4. 说明是否向后兼容；
5. 前后端约定切换时间；
6. 不要在未通知前端时直接删除或改名。

---

## 20. 旧后端和旧数据库处理原则

旧 `app.py`、`userDatabase.db` 和 `questionDatabase.db` 是历史实现和本地运行数据，不是新版 API 契约。

可以复用：

- 经过核验的真实题目内容；
- 合法且仍需保留的用户数据；
- 已有题号；
- 可验证的字段含义。

不能直接复用：

- Flask 模板渲染路由；
- 未经安全检查的密码重置流程；
- 直接写 SQLite 的数据库结构修改方式；
- 与 Vue 页面不一致的字段和跳转；
- 不明确的统计数据；
- 本地上传路径。

迁移步骤应是：

1. 备份旧数据库；
2. 确认新版数据模型；
3. 编写可重复执行的导入或数据迁移工具；
4. 在旧数据库副本上测试；
5. 对比迁移前后数量和关键字段；
6. 记录失败项；
7. 确认回滚方法；
8. 最后才迁移正式数据。

任何迁移测试都不能直接破坏仓库里的原始 `.db` 文件。

---

## 21. 推荐阅读

不需要一次全部读完，可以根据当前阶段阅读对应章节。

- [Django 官方入门](https://docs.djangoproject.com/en/stable/intro/)
- [Django 用户认证](https://docs.djangoproject.com/en/stable/topics/auth/)
- [Django 安全说明](https://docs.djangoproject.com/en/stable/topics/security/)
- [Django REST Framework 教程](https://www.django-rest-framework.org/tutorial/quickstart/)
- [Django REST Framework 认证](https://www.django-rest-framework.org/api-guide/authentication/)
- [Django REST Framework 权限](https://www.django-rest-framework.org/api-guide/permissions/)
- [Django REST Framework 筛选](https://www.django-rest-framework.org/api-guide/filtering/)
- [Django REST Framework 分页](https://www.django-rest-framework.org/api-guide/pagination/)
- [OWASP Authentication Cheat Sheet](https://cheatsheetseries.owasp.org/cheatsheets/Authentication_Cheat_Sheet.html)
- [OWASP Session Management Cheat Sheet](https://cheatsheetseries.owasp.org/cheatsheets/Session_Management_Cheat_Sheet.html)
- [OWASP Forgot Password Cheat Sheet](https://cheatsheetseries.owasp.org/cheatsheets/Forgot_Password_Cheat_Sheet.html)
- [PostgreSQL 官方文档](https://www.postgresql.org/docs/)

---

## 22. 最终目标

第一阶段完成后，应该达到以下结果：

- 学生可以浏览和筛选真实题库；
- 可以查看真实题目、答案和解析；
- 可以安全注册、登录和找回密码；
- 收藏、已做、题单和备注跟随账号保存；
- 刷新页面或更换设备后仍能读取账号数据；
- 管理员可以管理真实题目和基础分类；
- API 有稳定文档、统一错误、权限检查和自动化测试；
- 前端界面偏好仍可保存在浏览器，不与账号业务数据混在一起；
- 所有尚未实现的功能继续显示真实的待接入状态，不制造虚假能力。

请把“数据正确、权限正确、能被测试和维护”放在“功能数量多”之前。
