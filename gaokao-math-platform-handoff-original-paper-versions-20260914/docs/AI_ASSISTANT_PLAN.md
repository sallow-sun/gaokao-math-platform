# MathSea AI 学习助手开发计划

> 本文件是 AI 功能的长期状态与决策记录。后续开发开始前先阅读本文件；完成工作后更新阶段状态、验证结果和变更日志。不要只依赖聊天记录恢复上下文。

## 1. 产品定位

MathSea AI 不是一个独立的通用聊天框，而是连接当前页面、题库、标签体系、训练记录和站内操作的“数学学习操作层”。它的差异化价值来自：

1. 自动获得用户明确选择的页面上下文，不要求重复复制题目。
2. 使用 MathSea 标准知识点、题型与方法标签，而不是自由生成不可复用的词语。
3. 从站内真实题库检索、引用并操作题目，不伪造资源。
4. 将输出转成可执行结果，例如推荐题、标签建议、练习清单和组卷条件。
5. 用检索、结构化输出和确定性校验增强模型，而不是简单增加输出长度。

## 2. 当前商业与密钥策略

- 当前阶段：用户自带 API Key（BYOK）为主。
- 默认安全策略：Key 只保存在当前前端运行内存和单次中转请求中，不写入仓库、localStorage 或 sessionStorage。
- 浏览器不直连模型厂商；请求经过 MathSea 同源中转，限定官方 HTTPS 域名和 Chat Completions 路径。
- 后续盈利阶段：增加 MathSea 官方 API 额度、会员配额、限流和成本核算；BYOK 继续作为高级选项。
- 页面必须明确显示厂商、模型、是否真实调用、是否命中缓存、Token 用量和厂商请求 ID，不能让缓存结果冒充新调用。

## 3. 交互设计

### 3.1 全局入口

- 桌面端：右侧固定触发按钮，展开为 400–460px 的抽屉。
- 移动端：底部触发按钮，展开为全屏或大尺寸底部面板。
- 抽屉可在路由切换时保留设置与对话，但页面上下文必须随路由更新。
- 打印时完全隐藏。

### 3.2 抽屉结构

1. 顶栏：MathSea AI、当前模型、连接状态、关闭按钮。
2. 上下文区：当前页面或题目，可选择是否随请求发送。
3. 任务区：与页面相关的快捷任务卡片。
4. 对话区：用户与 AI 消息、结构化结果和错误状态。
5. 输入区：文本输入、发送、停止、清空。
6. 设置区：厂商预设、地址、模型、Key、超时、测试连接。
7. 调用区：缓存状态、请求 ID、输入/输出 Token、总 Token、耗时。

### 3.3 工作强度

- 快速：单次调用，适合连接测试、标签、摘要和简单提示。
- 标准：补充站内上下文与结构化约束，再进行一次模型调用。
- 深度：规划、解答、校验分阶段调用；执行前显示预计调用次数和成本。第一阶段只完成界面与协议，不启用多轮深度编排。

## 4. 页面任务矩阵

| 页面 | 第一批任务 | 后续任务 |
| --- | --- | --- |
| 首页/题库 | 自然语言检索入口、打开相似题页面 | 个性化训练计划 |
| 题目详情 | 解释当前题、渐进提示、相似题入口 | 多解法、变式题、思路检查 |
| 相似题推荐 | 复用统一 API 设置、显示调用信息 | API 精排、强制重新调用 |
| 上传题目 | 规则1.4本地检查、安全格式整理、标准分类建议 | OCR、重复题检测、标签评测闭环 |
| 练习/错题 | — | 错因分析、下一题推荐、薄弱点总结 |
| 自主组卷 | — | 覆盖度与难度审计、自然语言组卷 |
| 管理后台 | — | 标签审核、答案编辑审核、批量异常检测 |

## 5. 技术架构

```text
AI Drawer / 页面内快捷按钮
              ↓
Context Builder（当前路由、当前题目、用户选择）
              ↓
Task Orchestrator（任务模板、强度、缓存策略）
              ↓
Provider Adapter（DeepSeek / Qwen / OpenAI）
              ↓
同源安全中转（域名白名单、超时、大小限制）
              ↓
Response Normalizer（文本、结构化数据、usage、request id）
              ↓
站内工具与校验器（题库检索、标签词表、数学校验）
```

统一请求至少包含：`taskType`、`messages`、`context`、`provider`、`model`、`mode`、`timeoutSeconds`。统一响应至少包含：`content`、`model`、`usage`、`providerRequestId`、`elapsedMs`、`cached`。

## 6. 质量与安全原则

- 当前页面内容只有在界面明确显示“将随请求发送”时才进入模型请求。
- API 错误不得自动降级成离线结果，以免掩盖连接、计费或权限问题。
- 相同任务可以缓存，但必须显示“缓存命中”，并提供“强制重新调用”。
- AI 标签只能作为建议，必须经过标准词表映射和用户确认。
- AI 题解必须区分提示、思路和完整解答，默认不直接暴露完整答案。
- 作答检查与正式评分分开；没有确定性校验和评测集之前，不提供高风险自动评分。
- 不支持的模型能力必须明确提示，不伪造图片、工具或结构化输出支持。

## 7. 分阶段实施

### 第一阶段：全局助手基础设施（测试版已完成）

- [x] 全局可展开/缩回的 AI 抽屉，适配桌面和移动端。
- [x] 统一个人 API 设置，与相似题页面共享内存中的 Key 和模型配置。
- [x] 当前页面上下文；题目详情页提供题号、题干、标签和可选答案/解析。
- [x] 首批任务：自由对话、解释当前题、给出提示、进入相似题推荐。
- [x] 调用元数据：真实请求、缓存、Token、请求 ID、耗时。
- [x] 快速/标准/深度模式界面；第一阶段执行快速和标准，深度模式显示为规划中。
- [x] 浏览器回归、移动端、键盘关闭和打印隐藏。

验收：用户能够在任意学习页面打开抽屉；在题目详情页无需复制题干即可发起解释或提示；相似题页和抽屉共用 API 设置；每次调用能看出是否触发厂商、是否命中缓存和 Token 用量。

验证记录（2026-09-16）：

- 前端规范检查、23 项既有测试、4 项相似题算法测试和生产构建通过。
- AI 助手浏览器流程通过：题目上下文、Qwen 连接、真实调用、缓存标记、请求元数据、设置共享和移动端布局。
- 相似题浏览器流程通过：页面路由、同源中转、排序结果和移动端布局。
- 已在本机完成 Java 21 编译、可执行 JAR 打包与全量后端测试：40 项测试全部通过，其中包含 AI 地址安全策略测试和完整应用启动测试。

### 第二阶段：上传与标签助手（进行中，第一批测试版已完成）

- [已完成] 相似题页面支持上传单张题目图片；经格式、体积、压缩和隐私确认后，由多模态 API 识别题干并复用既有召回与排序。
- [已完成] 普通投稿页使用独立的可选入口；不打开 AI 时保持原上传流程。
- [已完成] 官方审核编辑区加入醒目的 AI 辅助分类入口，重点识别题型、D1～D7 难度和标准标签。
- [已完成] 共用结构化分析内核，可整理标题、题干、答案和解析；提示词禁止补做题目或凭空生成答案。
- [已完成] 题型、难度、标签在模型返回后再次按站内目录校验；未知标签只展示为未映射项，不能写入表单。
- [已完成] 展示字段级建议、分类依据、置信度、警告、未映射项、模型、耗时和 Token；用户逐项确认后才写入表单。
- [已完成] 社区投稿采用 AI 分类后可以把难度与标准标签送入官方审核；未使用 AI 时继续采用原默认难度和空标签。
- [已完成] 将上传规则1.4落为本地确定性规则引擎；无 API 也能检查必填字段、长度、题型、难度、标准TAG、公式定界符、材料段、HTML与图片路径。
- [已完成] AI 输出应用前再次执行规则引擎，并对数字、关系符号和 LaTeX 命令做受保护信息比对；凭空补写答案/解析或改变数学信息时阻止应用。
- [已完成] 官方“手动录题”沿用既有工作台，创建草稿后直接展开原编辑区；AI 作为独立按钮嵌入，不另建上传流程。
- [部分完成] 已提供仅处理换行、行尾空格和过多空行的确定性安全整理；更复杂的 LaTeX 修复与逐字符差异预览仍待完成。
- [待完成] 基于题库指纹的重复题候选，不让模型凭空判断重复。
- [待完成] 建立标签准确率评测集，记录用户接受、修改和拒绝比例。

第一批验证（2026-09-17）：前端规范检查、32 项单元测试和生产构建通过；普通投稿与官方编辑器的 AI 浏览器流程通过；后端 40 项测试全部通过。

### 第三阶段：学习辅导与题库工具（未开始）

- 分级提示：考点、突破口、下一步、完整思路、完整解答。
- 题库 RAG：检索类似题和已审核解法，再生成回答。
- 站内工具：加入题单、生成训练组、错题归因、自然语言组卷。
- 标准模式加入引用来源；深度模式加入规划和复核。

### 第四阶段：作答检查与质量体系（未开始）

- 用户上传文字、图片或分步作答。
- 区分正确步骤、明确错误、无法判断和缺失条件。
- 接入代数/数值等确定性校验工具。
- 建立人工标注评测集，跟踪误判率和拒答率。
- 达到质量门槛后再讨论分数评定；此前只称“思路检查”。

### 第五阶段：官方额度与运营（未开始）

- MathSea 官方 API 密钥托管、会员额度、并发限制与滥用防护。
- BYOK 与官方额度并存；任务级模型路由和故障切换。
- 费用仪表盘、调用审计、隐私说明和数据保留策略。
- 评估自建 embedding/reranker；只有在质量、隐私或长期成本证明合理后才评估自建生成模型。

## 8. 评测指标

- 相似题：Recall@K、Top-3 命中率、无可靠结果时的拒绝率。
- 标签：标准标签准确率、用户接受率、重复标签产生率。
- 辅导：提示有效率、直接泄露答案比例、用户继续训练比例。
- 作答检查：错误检出率、误报率、无法判断率。
- 系统：首字延迟、完整延迟、成功率、缓存命中率、每任务 Token 与费用。

## 9. 已有基础

- 独立相似题页面与首页入口。
- 离线特征、标准标签归一化、策略骨架排序和质量门槛。
- DeepSeek、Qwen、OpenAI 预设和同源开发中转。
- 生产后端中转骨架、厂商域名白名单和超时控制。
- API 与离线特征使用同一套标准化流程。

## 10. 变更日志

- 2026-09-16：建立长期计划；确认近期以 BYOK 为主，长期增加官方会员额度；第一阶段开始实施。
- 2026-09-16：完成第一阶段测试版；加入全局 AI 抽屉、共享 BYOK 设置、页面上下文、快捷任务、缓存与可核验的调用记录，并完成前端与浏览器回归。
- 2026-09-16：在 `D:\DevTools` 安装并校验 Temurin JDK 21 与 Maven 3.9.16；外部服务的网络客户端改为按需初始化，后端 40 项测试全部通过。
- 2026-09-16：AI 入口扩展到全部页面；浮钮支持鼠标和触摸拖动，拖入右上角收纳区后隐藏，并保留小型唤醒把手。
- 2026-09-16：收纳后的 AI 唤醒把手支持点击恢复，也支持从浏览器右边缘向左拖出并放置；网页浮层继续限制在浏览器可视区域内。
- 2026-09-16：桌面端 AI 对话框支持分别调整宽度、高度或同时缩放，并可一键恢复默认尺寸；尺寸始终限制在浏览器安全可视范围内。
- 2026-09-17：对话框视觉与首页统一，使用淡湖蓝、暖杏和青绿色雾状背景；重绘复原、设置、关闭、返回、发送和停止图标，并统一按钮边框、焦点与缩放把手。
- 2026-09-17：修复本地开发服务因受限启动而无法访问 Qwen 的问题；开发服务改为允许外部 HTTPS，并为 `EACCES/EPERM` 增加明确诊断提示。使用假 Key 验证请求已抵达百炼鉴权服务。
- 2026-09-17：第二阶段进入实施；完成普通用户可选 AI 上传、官方 AI 分类、标准目录二次校验、置信度与未映射项展示、逐项人工确认，以及社区投稿难度/标签的后端兼容支持。
- 2026-09-17：相似题推荐加入单张题目图片输入；浏览器端限制、压缩、预览与隐私确认后，由 Qwen/OpenAI 多模态模型识别题干和策略特征，再复用标准标签、召回、排序及质量门槛。
- 2026-09-17：修复图片识别成功但特征字段为空或命名不一致时的误报；兼容中英文、驼峰、嵌套和字符串数组格式，先执行标准标签映射，必要时仅用已识别题干向同一模型追加一次轻量特征校正。
- 2026-09-17：上传助手改为“规则引擎优先、AI建议其次”；规则1.4无需API即可检查，AI改动数学信息或补写缺失答案会被阻止；官方手动录题继续使用原工作台界面。
